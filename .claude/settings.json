{
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cat | jq -r '.user_prompt // .prompt // .message // \"\"' 2>/dev/null | grep -oP '^[a-zA-Z0-9_-]+(?=\\s+시작)' | head -1 | xargs -I {} curl -s 'http://localhost:40020/api/project/start?name={}' 2>/dev/null || true",
            "statusMessage": "프로젝트 시작 API 호출 중...",
            "async": true
          }
        ]
      }
    ]
  }
}
