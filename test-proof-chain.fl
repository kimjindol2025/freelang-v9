;; Proof-Chain 통합 테스트

;; v9-immutable.fl 로드
var immutable_result = test_immutable_store()
println("[RESULT] Immutable Store Test: " + str(immutable_result))

println("")

;; v9-hash-chain.fl 로드  
var chain_result = test_hash_chain()
println("[RESULT] Hash Chain Test: " + str(chain_result))

println("")

;; v9-audit-trail.fl 로드
var audit_result = test_audit_trail()
println("[RESULT] Audit Trail Test: " + str(audit_result))

println("")
println("========== PROOF-CHAIN PHASE 0 완료 ==========")
if immutable_result && chain_result && audit_result {
    println("✅ All tests passed (3/3)")
} else {
    println("❌ Some tests failed")
}
